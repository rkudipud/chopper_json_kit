{
  "$schema": "chopper/base/v1",
  "domain": "my_domain",
  "owner": "platform-team",
  "description": "Base-only project: no features selected. The project JSON simply points to this base and applies no feature overrides — use for minimal or locked-down flows.",
  "files": {
    "include": [
      "setup.tcl",
      "vars.tcl",
      "procs/core_procs.tcl"
    ]
  },
  "stages": [
    {
      "name": "setup",
      "load_from": "",
      "command": "-xt vw Imy_shell -B BLOCK -T setup",
      "exit_codes": [0],
      "steps": [
        "source setup.tcl"
      ]
    },
    {
      "name": "main",
      "load_from": "setup",
      "command": "-xt vw Imy_shell -B BLOCK -T main",
      "exit_codes": [0, 3, 5],
      "dependencies": ["setup"],
      "steps": [
        "source procs/core_procs.tcl",
        "run_main_flow"
      ]
    }
  ]
}
